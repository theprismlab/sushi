# Production. Tracks origin/main.
SUSHI_ENV=production
PORT=8100

JENKINS_URL=http://localhost:8080
JENKINS_JOB_PATH=job/sushi

PRISMSEQ_ROOT=/cmap/obelix/pod/prismSeq

# Outside the checkout, so a deploy or a rollback never touches run history.
SUSHI_UI_DB=/local/jenkins/sushi_ui.db
