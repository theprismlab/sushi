.Rhistory
sushi.Rproj
scratch/
.idea/
.DS_Store
/tests/assets/out/
/tests/assets/build/
.aider*
__pycache__/
*.pyc
# setuptools leaves these behind whenever the root project is built locally
# (e.g. `uv run` from the repo root, which resolves the root pyproject)
*.egg-info/
build/
.venv/
ui/frontend/node_modules/
ui/frontend/dist/
ui/backend/*.db
# Screen-type defaults edited from the UI; presets.yml is the seed in git.
ui/backend/presets.local.yml
# pidfile and log from bin/sushi-ui
run/
